---
description: lmdevv personal T3 fork. Follow the personal-t3-fork skill loop before saying work is done.
alwaysApply: true
---

# Personal fork

This checkout is `lmdevv/t3code`, not upstream `pingdotgg/t3code`. Keep the whole root `AGENTS.md`. The last section (`lmdevv personal fork`) overrides it.

At session start, read and follow:

1. `/home/zxcv/.cursor/skills/machine-context/SKILL.md`
2. `/home/zxcv/.cursor/skills/personal-t3-fork/SKILL.md`

Do not push to `origin`. Push to `personal` (`lmdevv/t3code`) `main`.

Do not tell the user the work is finished until the skill checklist is complete.